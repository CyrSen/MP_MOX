
  
  // Funktion zum Öffnen des Zu-kalt-Popups
function showPopup(popupId) {
  var popup = document.getElementById(popupId);
  popup.style.display = 'flex';
}

// Funktion zum Schließen des Zu-kalt-Popups
function closePopup(popupId) {
  var popup = document.getElementById(popupId);
  popup.style.display = 'none';
}

// Funktion zum Öffnen des Zu-heiss-Popups
function showPopup(popupId) {
  var popup = document.getElementById(popupId);
  popup.style.display = 'flex';
}

// Funktion zum Schließen des Zu-heiss-Popups
function closePopup(popupId) {
  var popup = document.getElementById(popupId);
  popup.style.display = 'none';
}

// Funktion zum Öffnen des Bürolärm-Popups
function showPopup(popupId) {
  var popup = document.getElementById(popupId);
  popup.style.display = 'flex';
}

// Funktion zum Schließen des Bürolärm-Popups
function closePopup(popupId) {
  var popup = document.getElementById(popupId);
  popup.style.display = 'none';
}


// Funktion zum Öffnen des Schlechte-Luft-Popups
function showPopup(popupId) {
  var popup = document.getElementById(popupId);
  popup.style.display = 'flex';
}

// Funktion zum Schließen des Schlechte-Luft-Popups
function closePopup(popupId) {
  var popup = document.getElementById(popupId);
  popup.style.display = 'none';
}


